# Production (example)
PORT=3009

# PostgreSQL
DB_HOST=<prod-db-host>
DB_PORT=5432
DB_USER=<prod-user>
DB_PASSWORD=<prod-password>
DB_NAME=<prod-db>

# External Services (set via server env or secret manager)
GOOGLE_MAPS_API_KEY=
GOOGLE_GEMINI_KEY=
OPENAI_API_KEY=

# Notes:
# - Do not commit real secrets.
# - Ensure CORS allows production origin (rabspoc.codexdiz.com).
