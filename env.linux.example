# Backend (Linux)
PORT=3009

# PostgreSQL (Linux host)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=rabspocdb

# External Services (set privately)
GOOGLE_MAPS_API_KEY=
GOOGLE_GEMINI_KEY=
OPENAI_API_KEY=

# Notes:
# - Use this on the Linux host only.
# - Do not commit real secrets.
